---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-spotify-db-claim
  namespace: etc
spec:
  storageClassName: nfs-client
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
