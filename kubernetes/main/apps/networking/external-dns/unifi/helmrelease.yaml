---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app external-dns-unifi
spec:
  interval: 30m
  chart:
    spec:
      chart: external-dns
      version: 1.14.4
      sourceRef:
        kind: HelmRepository
        name: external-dns
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  values:
    fullnameOverride: *app
    logLevel: debug
    provider:
      name: webhook
      webhook:
        image:
<<<<<<< HEAD
          repository: ghcr.io/buroa/external-dns-unifi-webhook
          tag: dev@sha256:9b72cfab6a322794ceb70f5e7d9c46ce026367308e9c2a76ea0bd0015ed8302b
=======
          repository: ghcr.io/kashalls/external-dns-unifi-webhook
          tag: main@sha256:08df76219cf735d19e98ca254ed55cc2a1b465fc9850d0b81b57ad12a094f8f2
>>>>>>> 7668c973bc3938d80210f2c74b5e2238f37f946f
        env:
          - name: UNIFI_HOST
            value: "https://10.1.0.131"
          - name: SERVER_HOST
            value: "0.0.0.0"
          - name: UNIFI_USER
            valueFrom:
              secretKeyRef:
                name: &secret external-dns-unifi-secret
                key: UNIFI_USER
          - name: UNIFI_PASS
            valueFrom:
              secretKeyRef:
                name: *secret
                key: UNIFI_PASS
          - name: LOG_LEVEL
            value: "debug"
          - name: UNIFI_SKIP_TLS_VERIFY
            value: "true"
    policy: sync
    sources:
      - ingress
      - service
    txtOwnerId: default
    txtPrefix: k8s.
    domainFilters:
      - ok8.sh
    excludeDomains:
      - internal
    serviceMonitor:
      enabled: false
    podAnnotations:
      secret.reloader.stakater.com/reload: *secret
