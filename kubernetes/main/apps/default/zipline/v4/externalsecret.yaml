---
# yaml-language-server: $schema=https://kubernetes-schemas.ok8.sh/external-secrets.io/externalsecret_v1beta1.json
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: zipline-v4
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: zipline-v4-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        # App
        CORE_SECRET: "{{ .ZIPLINE_CORE_SECRET }}"
        CORE_DATABASE_URL: "postgres://{{ .ZIPLINE_PG_USERNAME }}:{{ .ZIPLINE_PG_PASSWORD }}@postgres-rw.database.svc.cluster.local:5432/ziplinev4"
        # Postgres Init
        INIT_POSTGRES_DBNAME: ziplinev4
        INIT_POSTGRES_HOST: postgres-rw.database.svc.cluster.local
        INIT_POSTGRES_USER: "{{ .ZIPLINE_PG_USERNAME }}"
        INIT_POSTGRES_PASS: "{{ .ZIPLINE_PG_PASSWORD }}"
        INIT_POSTGRES_SUPER_PASS: "{{ .POSTGRES_SUPER_PASS }}"
  dataFrom:
    - extract:
        key: zipline
    - extract:
        key: cloudnative-pg
