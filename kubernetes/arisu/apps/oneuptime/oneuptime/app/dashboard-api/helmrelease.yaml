---
# yaml-language-server: $schema=https://kubernetes-schemas.kashall.dev/helm.toolkit.fluxcd.io/helmrelease_v2beta1.json
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: dashboard-api
  namespace: oneuptime
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 2
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  dependsOn:
    - name: redis
      namespace: oneuptime
    - name: postgres
      namespace: oneuptime
    - name: notification
      namespace: oneuptime
  values:
    image:
      repository: docker.io/oneuptime/dashboard-api
      tag: release
    env:
      PORT: &port 3002
    envFrom:
      - secretRef:
          name: common-server-secrets
      - secretRef:
          name: common-secrets
    service:
      main:
        ports:
          http:
            port: *port
    ingress:
      main:
        enabled: true
        ingressClassName: external
        hosts:
          - host: &host "oneuptime.kashall.dev"
            paths:
              - path: /api
                pathType: Prefix
        tls:
          - hosts:
            - *host
    resources:
      requests:
        cpu: 10m
        memory: 100Mi
      limits:
        memory: 500Mi

